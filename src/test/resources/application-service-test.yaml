spring:
  datasource:
    url: jdbc:mysql://localhost:3307/spring_gift_test?useSSL=false&allowPublicKeyRetrieval=true
    username: gift
    password: gift
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
